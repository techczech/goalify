interface:
  display_name: "Goalify"
  short_description: "Turn a project, PRD, or product interview into a Codex /goal task."
  default_prompt: "Interview me about this project, review the code and repo history, construct a compact PRD, then draft a Codex /goal prompt with product trace, checkpoints, validation commands, and stopping conditions."
